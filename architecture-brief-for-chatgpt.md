# بریف معماری و استقرار پروژه Youkart — برای اجرا با کمک ChatGPT

## زمینه پروژه

یک اپلیکیشن کارت ویزیت دیجیتال دارم با این ساختار کد (Node.js + Express + React/Vite):

- `digital-card/` — پروژه اصلی: لندینگ، پنل کاربر، پنل ادمین، ادیتور کارت، تیکت‌ها، اشتراک‌ها. فایل سرور: `server.ts`.
- `card-server/` — یک سرویس کاملاً جدا و سبک که فقط و فقط نمایش عمومی کارت‌ها را با SSR (Server-Side Rendering) و بهینه برای SEO انجام می‌دهد. هیچ کد داشبورد/ادمین/ادیتوری داخلش نیست.

هر دو پروژه به **یک دیتابیس PostgreSQL مشترک** وصل می‌شوند (نه دو دیتابیس جدا).

## وضعیت فعلی روی سرور (خیلی مهم)

نسخه‌ای که **الان روی سرور production من در حال اجراست، هنوز نسخه قدیمی با SQLite است** (فایل `data/app.db`)، نه نسخه جدید Postgres. یعنی:

- دیتای واقعی و زنده کاربرها (ثبت‌نام‌ها، تیکت‌ها، اشتراک‌ها، بازدیدها) الان داخل همین فایل SQLite روی سرور است.
- نسخه‌ی جدیدتر کد (که به‌جای SQLite از PostgreSQL استفاده می‌کند، async شده، و `card-server` هم به آن اضافه شده) آماده و از قبل تست شده است، ولی **هنوز روی سرور production دیپلوی نشده**.
- بنابراین قبل از هر چیز، باید **دیتای واقعی همین SQLite فعلی** (نه یک دیتابیس تست) به Postgres منتقل شود، بدون از دست رفتن هیچ داده‌ای.

## معماری دامنه‌ها (نهایی)

| دامنه | چیست | وضعیت |
|---|---|---|
| `youkart.ir` | سایت وردپرسی (لندینگ/بلاگ شرکتی) | از قبل هست، **دست نمی‌خورد** |
| `app.youkart.ir` | همین اپلیکیشن `digital-card` (داشبورد، ادمین، ادیتور، ورود/ثبت‌نام) | باید از SQLite به Postgres مهاجرت و روی این ساب‌دامین اجرا شود |
| `card.youkart.ir/username` | سرویس جدید `card-server` — فقط نمایش عمومی کارت‌ها با SSR، برای ایندکس شدن در گوگل | پروژه جدید، آماده دیپلوی |

هر سه روی یک سرور (یک VPS) هستند؛ Nginx باید بر اساس ساب‌دامین به پورت داخلی درست از این سه سرویس reverse proxy کند. وردپرس احتمالاً از قبل روی پورت ۸۰/۴۴۳ یا از طریق یک vhost دیگر سرو می‌شود — این باید دست‌نخورده بماند.

## پورت‌های داخلی هر سرویس

- `digital-card` (اپ اصلی، `server.ts`): پورت `3000` (قابل تنظیم با env `PORT`)
- `card-server`: پورت `4000`

## چک‌لیست دقیق کارهایی که باید روی سرور انجام شود (به همین ترتیب)

### مرحله ۱ — نصب و آماده‌سازی PostgreSQL
1. نصب PostgreSQL روی همان سرور (یا استفاده از یک Postgres مدیریت‌شده، هرکدام صرفه‌جویانه‌تر بود).
2. ساخت یک دیتابیس جدید، مثلاً `digitalcard`.
3. اجرای فایل `digital-card/db/schema.sql` روی این دیتابیس تا تمام جدول‌ها ساخته شود.

### مرحله ۲ — مهاجرت داده واقعی از SQLite فعلی به Postgres
1. از فایل `data/app.db` **فعلیِ روی سرور production** یک بک‌آپ بگیر (کپی امن جدا، قبل از هر کاری).
2. اسکریپت `digital-card/scripts/migrate-sqlite-to-postgres.ts` را روی همان فایل واقعی اجرا کن (این اسکریپت idempotent است، یعنی اجرای دوباره‌اش خطرناک نیست و داده تکراری نمی‌سازد).
3. بعد از اجرا، محتوای چند جدول کلیدی (`users`, `subscriptions`, `tickets`) را در Postgres با محتوای همان کاربرها در نسخه فعلی سایت مقایسه/راستی‌آزمایی کن تا مطمئن شوی همه‌چیز درست منتقل شده.

### مرحله ۳ — استقرار نسخه جدید `digital-card` (روی app.youkart.ir)
1. کد جدید (نسخه Postgres، نه نسخه قدیمی SQLite) را روی سرور بگذار.
2. `.env` این پروژه را با مقادیر واقعی پر کن — حداقل شامل: `DATABASE_URL` (به همان Postgres مرحله ۱)، `JWT_SECRET`، `ADMIN_USERNAME`/`ADMIN_EMAIL`/`ADMIN_PASSWORD`/... (همانی که الان استفاده می‌شود)، `PORT=3000`، `CARD_SERVER_URL=http://localhost:4000`، `INTERNAL_SECRET` (یک مقدار تصادفی طولانی، دلخواه).
3. `npm install` و سپس `npm run build`.
4. با PM2 بالا بیاور: `pm2 start ecosystem.config.js --env production` (نام پردازه در PM2: `digital-card`، روی پورت ۳۰۰۰).

### مرحله ۴ — استقرار `card-server` (روی card.youkart.ir)
1. پوشه `card-server` را روی سرور قرار بده — این پروژه کاملاً مستقل و خودکفاست (کامپوننت نمایش کارت و تمام وابستگی‌هایش از قبل داخل خودش کپی شده‌اند)، پس **دیگر نیازی نیست حتماً کنار `digital-card` باشد**؛ در هر مسیری روی سرور قرار بگیرد کار می‌کند.
2. `.env` این سرویس را پر کن: `DATABASE_URL` (همان Postgres مرحله ۱، دقیقاً همان‌طور)، `PORT=4000`، `SITE_BASE_URL=https://card.youkart.ir`، `UPLOADS_BASE_URL=https://app.youkart.ir` (چون فایل‌های آپلودی از سرور اصلی سرو می‌شوند)، `INTERNAL_SECRET` (دقیقاً همان مقداری که در `.env` پروژه `digital-card` گذاشتی).
3. `npm install`، سپس `npm run build` (که هم CSS و هم باندل SSR را می‌سازد).
4. با PM2 بالا بیاور: `pm2 start ecosystem.config.js --env production` (نام پردازه: `card-server`، روی پورت ۴۰۰۰).

### مرحله ۵ — تنظیم Nginx (این بخش را من می‌خواهم خودم انجام بدهم، فقط راهنمایی بده نه اینکه خودت فایل‌های واقعی سرور را عوض کنی)
باید سه بلاک `server` جدا در Nginx تعریف شود:
- `youkart.ir` → همانی که الان برای وردپرس هست، دست‌نخورده.
- `app.youkart.ir` → `proxy_pass http://localhost:3000;`
- `card.youkart.ir` → `proxy_pass http://localhost:4000;`

هر سه روی همان IP سرور، با گواهی SSL جدا یا یک گواهی wildcard برای `*.youkart.ir`.

### مرحله ۶ — تست نهایی
1. `https://app.youkart.ir` → لاگین با اکانت ادمین قبلی باید کار کند (چون همان یوزر/پسورد از دیتای مهاجرت‌شده می‌آید).
2. `https://card.youkart.ir/یک-یوزرنیم-واقعی` → کارت باید همان‌طور نمایش داده شود که در `app.youkart.ir/card/یوزرنیم` نمایش داده می‌شد.
3. `https://card.youkart.ir/sitemap.xml` و `https://card.youkart.ir/robots.txt` را چک کن.
4. یکبار از داخل پنل کاربر، کارت را ویرایش/ذخیره کن و مطمئن شو نسخه‌ی نمایش‌داده‌شده در `card.youkart.ir` هم به‌روز می‌شود (حداکثر تا ۶۰ ثانیه طول می‌کشد، چون کش دارد).

## نکات مهم برای هوش مصنوعی که این کار را انجام می‌دهد

- **قبل از مهاجرت، حتماً از دیتابیس SQLite فعلی بک‌آپ بگیر.** این دیتای زنده و واقعی کاربرهاست، نباید ریسک از دست رفتنش را پذیرفت.
- کد پروژه `digital-card` که الان روی سرور است نسخه قدیمی (SQLite) است؛ باید با نسخه جدید (که از `db.prepare` سینک به `pg` async تبدیل شده) جایگزین شود، نه اینکه فقط دیتابیسش عوض شود.
- `card-server` کاملاً مستقل و خودکفاست — کامپوننت نمایش کارت (`CardPreview.tsx`) و تمام وابستگی‌هایش (types، توابع کمکی، لوگوها، CSS) به‌صورت فیزیکی داخل خود `card-server` کپی شده‌اند. یعنی روی Render/هر پلتفرمی می‌توان آن را کاملاً جدا (با `root directory` و `node_modules` خودش، بدون هیچ دسترسی‌ای به پوشه `digital-card`) build و اجرا کرد. تنها نقطه اشتراکش با پروژه اصلی، همان یک `DATABASE_URL` مشترک است.
- مسیر عمومی کارت در پروژه اصلی (`app.youkart.ir/card/username`) فعلاً دست‌نخورده می‌ماند و حذف نمی‌شود؛ فقط `card.youkart.ir/username` به‌عنوان نسخه SEO-بهینه‌شده و سریع‌تر اضافه می‌شود.
